import React from 'react';
import {observer} from 'mobx-react-lite';

import './index.less';

const HomeDemo = () => {
    return <div className='home-mobx-root'>Demo</div>;
};

export default observer(HomeDemo);
